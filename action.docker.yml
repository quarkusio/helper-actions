name: 'Action helpers'
description: 'GitHub Actions helpers for the Quarkus project'
inputs:
  github-token:
    description: 'GitHub token'
    required: false
runs:
  using: 'docker'
  image: 'docker://ghcr.io/quarkusio/action-helpers:main'